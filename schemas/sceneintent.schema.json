{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "oeb:sceneintent:1.0.0",
  "title": "SceneIntent",
  "description": "The LLM output boundary (decision 2026-07-04: SceneIntent only). High-level intent with NO asset, clip, mark, or camera IDs — the deterministic resolver maps intent to approved assets and emits a SceneSpec. Free-text fields are intent vocabulary, not identifiers.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "scene_id", "location_tag", "time_of_day", "actors", "beats", "shot_intents"],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "scene_id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
    "location_tag": {
      "type": "string",
      "description": "Semantic location (e.g. 'small_bar_interior'); the resolver maps it to a set asset.",
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "time_of_day": { "enum": ["morning", "day", "evening", "night"] },
    "actors": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["actor_id", "role_tag"],
        "properties": {
          "actor_id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
          "role_tag": {
            "type": "string",
            "description": "Semantic role (e.g. 'protagonist', 'bartender'); resolver maps to a character asset.",
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "arrives": {
            "type": "boolean",
            "default": false,
            "description": "Actor enters on foot at scene start: the resolver emits move cues from the role's mapped entrance (entry mark -> approach -> spawn mark) in the first shot, before any dialogue. Requires an 'entrance' block on the role in the resolver map."
          },
          "departs": {
            "type": "boolean",
            "default": false,
            "description": "Actor exits on foot at scene end: the resolver emits rise + walk-out move cues (spawn mark -> approach -> entry mark, reverse of the entrance) in the LAST shot the actor appears in, after any dialogue. Requires the role's 'entrance' block (rise_clip/rise_duration included)."
          }
        }
      }
    },
    "beats": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "description"],
        "properties": {
          "order": { "type": "integer", "minimum": 0 },
          "description": { "type": "string", "minLength": 1 },
          "actor_ids": {
            "type": "array",
            "items": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" }
          },
          "dialogue": {
            "type": "array",
            "description": "Approved source lines for this beat, verbatim — the LLM transcribes, it does not write.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["actor_id", "text"],
              "properties": {
                "actor_id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
                "text": { "type": "string", "minLength": 1 }
              }
            }
          },
          "moves": {
            "type": "array",
            "description": "Mid-scene relocations the action text describes (e.g. 'flies into the asteroid field') — decided by the director (tools/director.py), not a Producer heuristic. The resolver moves the actor from wherever it currently is to to_mark over duration_s, transform-only (no clip) unless clip_id is given. Distinct from arrives/departs, which only cover scene-start/scene-end walk-in/walk-out.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["actor_id", "to_mark"],
              "properties": {
                "actor_id": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
                "to_mark": {
                  "type": "string",
                  "description": "Must be one of the resolved location's actual marks — the resolver does not invent one.",
                  "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$"
                },
                "clip_id": { "type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$" },
                "duration_s": { "type": "number", "exclusiveMinimum": 0, "default": 2.0 }
              }
            }
          }
        }
      }
    },
    "shot_intents": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "framing", "beat_orders"],
        "properties": {
          "order": { "type": "integer", "minimum": 0 },
          "framing": {
            "enum": ["establishing", "two_shot", "close_on", "medium_on"],
            "description": "Grammar-level framing intent; resolver maps to a camera_setup ID."
          },
          "subject_actor_id": {
            "type": "string",
            "description": "Required by the resolver when framing is close_on or medium_on.",
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "beat_orders": {
            "type": "array",
            "description": "Which beats this shot covers. REQUIRED (2026-07-06): an uncovered shot resolves empty and silently drops its beats' dialogue; requiring it also forces grammar-constrained LLM decoding to emit it.",
            "minItems": 1,
            "items": { "type": "integer", "minimum": 0 }
          }
        }
      }
    }
  }
}
